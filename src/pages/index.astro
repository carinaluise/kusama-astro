---
import Layout from '../layouts/layout.astro';

import Hero from '../components/Hero';
import BoxItem from '../components/BoxItem';
import Vortex from '../components/Vortex';
import Canary from '../components/Canary';
import NewsletterCTA from '../components/NewsletterCTA';
import Button from '../components/Button';

import { Image } from 'astro:assets';
import Mesh from '../images/index/mesh.png';
import Playground from '../images/index/playground.png';

import indexData from '../data/pages/index';
---

<Layout title="Kusama">
  <Hero client:load />
  <article>
    <section>
      <h2 class="text-2xl max-w-[20ch] mb-16">The Scalable, Multichain Network for Radical Innovation.</h2>
      <p class="relative max-w-[50ch] md:left-24 lg:max-w-1/2 lg:left-1/2">
        Unprecedented interoperability and scalability for blockchain developers who want to quickly push the limits of
        what’s possible. Built using Substrate with nearly the same codebase and industry-leading multichain
        infrastructure as Kusama’s cousin, Polkadot.
      </p>
    </section>
    <Vortex client:load />
    <section>
      <h2>Origin of Kusama</h2>
      <div class="md:w-3/4 lg:w-1/2">
        <p>
          The relationship between society and technology has deteriorated to the point where large entities routinely
          stretch and overstep their authority.
        </p>
        <p>
          Kusama is a network built as a risk-taking, fast-moving ‘canary in the coal mine’ for its cousin Polkadot.
          It's a living platform built for change agents to take back control, spark innovation and disrupt the status
          quo.
        </p>
      </div>
    </section>
    <BoxItem
      client:load
      title="What is Kusama?"
      p1="Kusama is a scalable network of specialized blockchains built using Substrate and nearly the same codebase as Polkadot. The network is an experimental development environment for teams who want to move fast and innovate on Kusama, or prepare for deployment on Polkadot."
      p2="Kusama was founded in 2019 by Gavin Wood, founder of Polkadot and co-founder and former CTO of Ethereum."
    />
    <div>
      <section class="lg:sticky lg:top-32">
        <h2 class="text-2xl z-20 relative top-48 w-1/2 lg:w-[350px]">Launch your custom blockchain</h2>
        <Image alt="" class="mx-auto opacity-40 w-[85%]" src={Mesh} />
      </section>
      <section>
        <ul class="md:w-3/4 mt-4 lg:w-1/2 relative md:left-24 lg:left-1/2">
          {
            indexData.customBlockchain.map((item) => {
              return (
                <li class="mb-16 lg:last:pb-[80vh]">
                  <div class="flex gap-4 mb-4">
                    <Image class="w-8" alt="" src={item.img} />
                    <h3 class="mb-0">{item.title}</h3>
                  </div>
                  <p class="w-[95%]">{item.description}</p>
                </li>
              );
            })
          }
        </ul>
      </section>
    </div>
  </article>
  <section class="flex justify-center">
    <a href="https://polkadot.network/development/build/">
      <Button>Ready to start? Contact our team.</Button>
    </a>
  </section>
  <section>
    <div class="lg:sticky lg:top-96">
      <h2 class="text-2xl mb-24">Bring <br /> to your project</h2>
    </div>
    <ul class="md:w-3/4 md:left-1/4 lg:w-1/2 relative lg:left-1/2 lg:mt-96 lg:pb-72">
      {
        indexData.bringToYourProject.map((item) => (
          <li class="mb-16">
            <h3 class="mb-4">{item.title}</h3>
            <p class="mb-8">{item.description}</p>
          </li>
        ))
      }
    </ul>
  </section>
  <Canary client:load />
  <section class="lg:flex w-full gap-24 items-baseline">
    <h3 class="mb-16 lg:w-1/2">Parachains and Applications</h3>
    <ul class="">
      {
        indexData.parachainsAndApplications.map((item) => (
          <li class="w-1/2 lg:w-full relative left-[40%] md:left-1/2">
            <p>{item}</p>
          </li>
        ))
      }
    </ul>
  </section>
  <section class="flex justify-center">
    <a
      href="https://wiki.polkadot.network/docs/en/build-build-with-polkadot#what-is-the-difference-between-building-a-parachain-a-parathread-or-a-smart-contract"
    >
      <Button>Why Build a Parachain</Button>
    </a>
  </section>
  <section class="lg:flex w-full gap-24 items-baseline">
    <h3 class="mb-16 lg:w-1/2">KSM Token Utilization</h3>
    <ul>
      {
        indexData.ksmTokenUtilization.map((item) => (
          <li class="w-1/2 lg:w-3/4 relative left-[14%] md:left-1/3">
            <p>{item}</p>
          </li>
        ))
      }
    </ul>
  </section>
  <section class="lg:flex">
    <h3 class="mb-16 lg:w-1/2">Built with Substrate</h3>
    <div class="lg:w-1/2 md:w-3/4 md:mx-auto">
      <Image alt="" class="mb-8" src={Playground} />
      <p class="mb-8">
        Your Substrate Blockchain is natively compatible with Kusama, making it simple to secure your blockchain and
        communicate with Kusama's network.
      </p>
      <a href="https://substrate.io/">
        <Button varient="white">Learn More</Button>
      </a>
      <a href="https://docs.substrate.io/">
        <Button>Substrate Playground</Button>
      </a>
    </div>
  </section>
  <section>
    <h2 class="text-2xl text-center mb-12">Take Flight</h2>
    <div class="flex flex-wrap justify-center gap-16 md:gap-12 lg:gap-16">
      {
        indexData.boxItems.map((item) => (
          <BoxItem client:load structure="small" title={item.title} p1={item.link1} p2={item.link2} />
        ))
      }
    </div>
  </section>
  <NewsletterCTA client:idle />
</Layout>
